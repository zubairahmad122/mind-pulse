// Local Expo module: native Android alarm (exact AlarmManager alarm, ringing
// foreground service and full-screen wake-up activity).
//
// The native module is consumed through the service layer in
// `src/services/nativeAlarm.ts` via `requireOptionalNativeModule('MindPulseAlarm')`,
// so this file only documents the module and triggers autolinking.
export {};
